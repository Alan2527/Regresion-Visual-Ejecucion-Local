name: Regresión Visual - DESKTOP PROD

on:
  workflow_dispatch:
    inputs:
      version_number:
        description: 'Número de versión a testear'
        required: true
        default: '0'

permissions:
  contents: write # Necesario para hacer el commit del reporte
  pages: write    
  id-token: write 

jobs:
  run_regression_and_deploy:
    runs-on: self-hosted
    # Asegurarse de que el environment 'github-pages' esté configurado en la repo
    environment:
      name: github-pages
      url: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/

    steps:
      - name: ⬇️ Checkout del código (CON TOKEN EXPLÍCITO)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 
          token: ${{ secrets.GITHUB_TOKEN }}
          
      # 1. EJECUCIÓN DEL SCRIPT
      - name: 🚀 Ejecutar Regresión Visual
        shell: cmd
        run: |
          @echo off
          REM La configuración de UTF-8 es CRÍTICA
          chcp 65001
          set PYTHONIOENCODING=utf-8
          
          REM Limpiar reportes viejos 
          if exist "Reportes HTML - TN - DESKTOP - PROD" (
            rmdir /s /q "Reportes HTML - TN - DESKTOP - PROD"
          )
          
          py regre_visual_tn_desk_prod.py ${{ inputs.version_number }}

      # 2. Renombrar y Desplegar el Reporte a la Rama 'gh-pages'
      - name: 📝 Renombrar a index.html y Commit a gh-pages (PROCESO ROBUSTO DE GIT)
        run: |
          $reportDir = "Reportes HTML - TN - DESKTOP - PROD"
          $ErrorActionPreference = "Stop" 
          
          # 2.1. Renombrar el HTML a index.html (esencial para Pages)
          $reportFile = Get-ChildItem -Path $reportDir -Filter "Reporte_DOM_Estructural_*.html"
          if ($reportFile) {
              Rename-Item -Path $reportFile.FullName -NewName "index.html"
          } else {
              Write-Host "Error: No se encontró el archivo de reporte HTML."
              exit 1
          }
          
          # 2.2. Configurar Git para la autoría
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # 2.3. Despliegue de Git MÁS ROBUSTO (Corrección de la causa del 404)
          git checkout main -f
          git branch -D gh-pages -f | Out-Null
          git checkout --orphan gh-pages
          
          # 2.4. Limpiar el directorio raíz (borrar código fuente)
          Get-ChildItem -Path . -Exclude ".git", "$reportDir" -Force | Remove-Item -Recurse -Force
          
          # 2.5. Mover contenido del reporte a la raíz
          Copy-Item -Path "$reportDir\*" -Destination . -Recurse -Force
          Remove-Item -Path $reportDir -Recurse -Force
          
          # 2.6. Commit y Push
          git add .
          git commit -m "Deploy: Reporte v${{ github.event.inputs.version_number }} (Run ID ${{ github.run_id }})"
          # Forzar el push a la rama 'gh-pages'
          git push -f origin gh-pages
        shell: powershell
        
      # 3. Notificación a Slack (CORREGIDO: Eliminado el campo 'text' redundante)
      - name: 🔔 Notificación de Reporte a Slack
        uses: slackapi/slack-github-action@v1.23.0
        if: always() 
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }} 
        with:
          payload: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":chart_with_upwards_trend: *Ejecución de Regresión (DESKTOP PROD)* - Estado: ${{ job.status == 'success' && '✅ Éxito' || '❌ Falló' }} - Versión: ${{ github.event.inputs.version_number }}"
                  }
                },
                {
                  "type": "divider"
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*URL del Reporte:* <https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/?v=${{ github.run_id }}| Abrir Reporte :link:>"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Ver Ejecución:* <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|Enlace a GitHub>"
                  }
                }
              ]
            }
