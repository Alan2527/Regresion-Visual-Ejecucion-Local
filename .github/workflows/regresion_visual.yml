name:  Regresión Visual - DESKTOP PROD

# Se ejecuta manualmente, solicitando la versión
on:
  workflow_dispatch:
    inputs:
      version_number:
        description: 'Número de versión a testear'
        required: true
        default: '0'

# Permisos necesarios para poder subir el reporte a GitHub Pages
permissions:
  contents: write

jobs:
  run_regression_and_deploy:
    runs-on: self-hosted # Ejecuta en tu máquina local

    steps:
      - name: ⬇️ Checkout del código
        uses: actions/checkout@v4
          
      # 1. EJECUCIÓN DEL SCRIPT (Con corrección de Encoding)
      - name: 🚀 Ejecutar Regresión Visual (UTF-8 Fix)
        shell: cmd # Forzamos CMD para estabilidad en la configuración de entorno
        run: |
          @echo off
          REM Forzar UTF-8 en CMD (chcp 65001) y en Python (PYTHONIOENCODING)
          chcp 65001
          set PYTHONIOENCODING=utf-8
          
          REM Limpiar reportes viejos (lo movemos aquí para que use CMD)
          if exist "Reportes HTML - TN - DESKTOP - PROD" (
            rmdir /s /q "Reportes HTML - TN - DESKTOP - PROD"
          )
          
          REM Ejecución del script
          py regre_visual_tn_desk_prod.py ${{ inputs.version_number }}

      # 2. Renombrar el HTML generado
      - name: 📝 Renombrar a index.html para Pages
        shell: powershell
        run: |
          # Navegamos a la carpeta de reportes generada por el script.
          cd "Reportes HTML - TN - DESKTOP - PROD"
          
          # Buscamos el archivo HTML que contiene la marca de tiempo y lo renombramos a index.html
          Get-ChildItem -Filter "Reporte_DOM_Estructural_*.html" | Rename-Item -NewName "index.html"
          
      # 3. Backup (Opcional pero Recomendado): Subir como Artifact ZIP
      - name: 📦 Subir Artefacto (Backup)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Backup-Reporte-v${{ inputs.version_number }}-${{ github.run_id }}
          path: "Reportes HTML - TN - DESKTOP - PROD"

      # 4. Publicación en GitHub Pages
      - name: 🌐 Publicar Reporte Web (GitHub Pages)
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: "Reportes HTML - TN - DESKTOP - PROD"
          branch: gh-pages
