name: Regresión Visual - DESKTOP PROD

on:
  # Permite ejecutar el workflow manualmente y pasar el número de versión.
  workflow_dispatch:
    inputs:
      version_number:
        description: 'Número de versión a testear'
        required: true
        default: '0'

permissions:
  contents: read     # Necesario para el checkout
  pages: write       # Necesario para desplegar en GitHub Pages
  id-token: write    # Necesario para la autenticación de despliegue

jobs:
  # 1. JOB DE EJECUCIÓN (Corre en tu Runner de Windows y sube el artefacto)
  run_regression:
    runs-on: self-hosted 
    steps:
      - name: ⬇️ Checkout del código
        uses: actions/checkout@v4
          
      # 1.1. EJECUCIÓN DEL SCRIPT
      - name: 🚀 Ejecutar Regresión Visual
        shell: cmd
        run: |
          @echo off
          REM La configuración de UTF-8 es CRÍTICA
          chcp 65001
          set PYTHONIOENCODING=utf-8
          
          REM Limpiar reportes viejos (en el runner local)
          if exist "Reportes HTML - TN - DESKTOP - PROD" (
            rmdir /s /q "Reportes HTML - TN - DESKTOP - PROD"
          )
          
          py regre_visual_tn_desk_prod.py ${{ inputs.version_number }}
          
      # 1.2. PREPARACIÓN DEL ARTEFACTO
      - name: 🛠️ Preparar Archivos para Despliegue
        shell: powershell
        run: |
          $reportDir = "Reportes HTML - TN - DESKTOP - PROD"
          
          # Renombrar a index.html
          $reportFile = Get-ChildItem -Path $reportDir -Filter "Reporte_DOM_Estructural_*.html"
          if ($reportFile) {
              Rename-Item -Path $reportFile.FullName -NewName "index.html"
          } else {
              Write-Host "Error: No se encontró el archivo de reporte HTML. Deteniendo."
              exit 1
          }
          # Mover todo el contenido al directorio _site
          Move-Item -Path $reportDir -Destination "_site" -Force
      
      # 1.3. SUBIR EL REPORTE COMO ARTEFACTO ESTÁNDAR (Compatible con Windows)
      - name: ⬆️ Subir Artefacto para Transferencia
        uses: actions/upload-artifact@v4
        with:
          name: pages-report-artifact # Nombre del artefacto
          path: _site # Sube el contenido de _site
          retention-days: 1 

  # 2. JOB DE DESPLIEGUE (Corre en un Runner de GitHub compatible)
  deploy_pages:
    environment:
      name: github-pages
      # Usa la URL que te dará el paso de despliegue
      url: ${{ steps.deployment.outputs.page_url }} 
    
    # Este paso debe correr en un runner alojado en GitHub (Linux/Ubuntu)
    runs-on: ubuntu-latest 
    needs: run_regression # Espera a que el job anterior haya terminado

    steps:
      # 2.1. DESCARGAR EL ARTEFACTO CREADO EN EL JOB ANTERIOR
      - name: ⬇️ Descargar Reporte (del runner de Windows)
        uses: actions/download-artifact@v4
        with:
          name: pages-report-artifact
          # Importante: Si no se especifica 'path', el contenido se descarga en una carpeta con el mismo nombre del artefacto.

      # 2.2. SUBIR EL ARTEFACTO CON EL FORMATO DE PAGES
      - name: ⬆️ Subir Artefacto para Pages (Formato Oficial)
        uses: actions/upload-pages-artifact@v3 
        with:
          # CORRECCIÓN: Apuntamos a la carpeta que download-artifact creó con el contenido
          path: pages-report-artifact 
          
      # 2.3. DESPLEGAR A PAGES Y ESPERAR CONFIRMACIÓN (FIX de la URL)
      - name: 🚀 Desplegar a GitHub Pages y Esperar URL
        id: deployment
        uses: actions/deploy-pages@v4 # Espera a que la URL esté 100% actualizada

      # 2.4. NOTIFICACIÓN A SLACK (USA LA URL CONFIRMADA)
      - name: 🔔 Notificación a Slack
        uses: slackapi/slack-github-action@v1.23.0
        if: always() 
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }} 
        with:
          payload: |
            {
              "text": "*Reporte de Regresión Visual (Desktop PROD)* - Estado: ${{ needs.run_regression.result == 'success' && 'Éxito' || 'Fallo' }}",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":chart_with_upwards_trend: *Ejecución de Regresión (DESKTOP PROD)*\n\n*Versión Testeada:*\n${{ github.event.inputs.version_number }}\n\n*Estado Final:*\n*${{ needs.run_regression.result == 'success' && '✅ Éxito' || '❌ Falló' }}*"
                  }
                },
                {
                  "type": "divider"
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*URL del Reporte:* <${{ steps.deployment.outputs.page_url }}| Abrir Reporte :link:>"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Ver Ejecución:* <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|Enlace a GitHub>"
                  }
                }
              ]
            }
