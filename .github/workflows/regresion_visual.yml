name: 👁️ Regresión Visual - DESKTOP PROD

on:
  workflow_dispatch:
    inputs:
      version_number:
        description: 'Número de versión a testear'
        required: true
        default: '0'

# Permisos requeridos por las acciones oficiales de GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  run_regression_and_deploy:
    runs-on: self-hosted
    environment:
      name: github-pages # Define el entorno de despliegue
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: ⬇️ Checkout del código
        uses: actions/checkout@v4
          
      # 1. EJECUCIÓN DEL SCRIPT (UTF-8 Fix)
      - name: 🚀 Ejecutar Regresión Visual (UTF-8 Fix)
        shell: cmd
        run: |
          @echo off
          REM Forzar UTF-8 y limpiar reportes
          chcp 65001
          set PYTHONIOENCODING=utf-8
          if exist "Reportes HTML - TN - DESKTOP - PROD" (
            rmdir /s /q "Reportes HTML - TN - DESKTOP - PROD"
          )
          
          py regre_visual_tn_desk_prod.py ${{ inputs.version_number }}

      # 2. Renombrar el HTML generado a index.html
      - name: 📝 Renombrar a index.html para Pages
        shell: powershell
        run: |
          cd "Reportes HTML - TN - DESKTOP - PROD"
          Get-ChildItem -Filter "Reporte_DOM_Estructural_*.html" | Rename-Item -NewName "index.html"
          
      # 3. CONFIGURACIÓN CRÍTICA DEL ENTORNO DE WINDOWS
      - name: 🔨 Asegurar Utilitarios Unix y Fix Path Conversion
        shell: powershell
        run: |
          # 3.1: Añadir utilidades de Git Bash (tar, bash) al PATH
          $gitBinPath = "C:\Program Files\Git\usr\bin"
          "$gitBinPath" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          
          # 3.2: CORRECCIÓN DE RUTAS CON ESPACIOS (MSYS_NO_PATHCONV=1)
          # Esto previene que bash rompa C:\Users\Alan Herrera...
          echo "MSYS_NO_PATHCONV=1" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      # 4. Configuración del Entorno de Pages (Utiliza el nuevo PATH y ENV)
      - name: ⚙️ Configurar GitHub Pages
        uses: actions/configure-pages@v5

      # 5. Cargar el Reporte para Despliegue (Aquí es donde ocurre el error, pero con el fix debe pasar)
      - name: ⬆️ Cargar Reporte para Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'Reportes HTML - TN - DESKTOP - PROD'

      # 6. Desplegar a GitHub Pages
      - name: 🌐 Desplegar a GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
