name:  Regresión Visual - DESKTOP PROD

on:
  workflow_dispatch:
    inputs:
      version_number:
        description: 'Número de versión a testear'
        required: true
        default: '0'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  run_regression_and_deploy:
    runs-on: self-hosted
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: ⬇️ Checkout del código
        uses: actions/checkout@v4
          
      # 1. EJECUCIÓN DEL SCRIPT
      - name: 🚀 Ejecutar Regresión Visual
        shell: cmd
        run: |
          @echo off
          REM La configuración de UTF-8 es CRÍTICA
          chcp 65001
          set PYTHONIOENCODING=utf-8
          
          REM Limpiar reportes viejos 
          if exist "Reportes HTML - TN - DESKTOP - PROD" (
            rmdir /s /q "Reportes HTML - TN - DESKTOP - PROD"
          )
          
          py regre_visual_tn_desk_prod.py ${{ inputs.version_number }}

      # 2. Renombrar el HTML generado a index.html
      - name: 📝 Renombrar a index.html para Pages
        shell: powershell
        run: |
          cd "Reportes HTML - TN - DESKTOP - PROD"
          Get-ChildItem -Filter "Reporte_DOM_Estructural_*.html" | Rename-Item -NewName "index.html"
          
      # 3. Configuración del Entorno de Pages
      - name: ⚙️ Configurar GitHub Pages
        uses: actions/configure-pages@v5

      # 4. Cargar el Reporte para Despliegue
      - name: ⬆️ Cargar Reporte para Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'Reportes HTML - TN - DESKTOP - PROD'

      # 5. Desplegar a GitHub Pages
      - name: 🌐 Desplegar a GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      # 6. Notificación a Slack con la URL (¡ACCIÓN FINAL Y ESTABLE!)
      - name: 🔔 Notificación de Reporte a Slack (Final)
        uses: rtCamp/action-slack-notify@v2
        if: always() 
        env:
          # Esta acción usa una variable de entorno para el Webhook
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_USERNAME: GitHub Actions Report
          SLACK_ICON: https://cdn-icons-png.flaticon.com/512/25/25231.png # Icono de GitHub
          SLACK_COLOR: ${{ job.status == 'success' && 'good' || 'danger' }} 
          SLACK_MESSAGE: |
            :chart_with_upwards_trend: *Estado de Regresión Visual (DESKTOP PROD)*
            *Versión Testeada:* ${{ github.event.inputs.version_number }}
            *Estado Final:* ${{ job.status }}
            
            :link: *URL del Reporte de TN:* <${{ steps.deployment.outputs.page_url }}|Ver Reporte Completo>
            :github: *Ver Ejecución en Actions:* <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|Enlace a GitHub>
